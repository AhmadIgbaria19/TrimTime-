# Copy to hosts.ini (gitignored) after terraform apply.
# ansible_host must be the Elastic IP from: terraform output elastic_ip
# This example host is not a real server.

[trimtime]
trimtime-app ansible_host=invalid.example ansible_user=ubuntu
