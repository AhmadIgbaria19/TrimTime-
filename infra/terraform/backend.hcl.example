# Copy to backend.hcl (gitignored) after the bootstrap bucket exists.
# Partial backend config — used as: terraform init -backend-config=backend.hcl

bucket       = "REPLACE-with-the-bootstrap-bucket-name"
key          = "prod/terraform.tfstate"
region       = "eu-central-1"
encrypt      = true
use_lockfile = true
